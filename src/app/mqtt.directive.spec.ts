import { MqttDirective } from './mqtt.directive';

describe('MqttDirective', () => {
  it('should create an instance', () => {
    const directive = new MqttDirective();
    expect(directive).toBeTruthy();
  });
});
